[package]
name = "palmier-auth"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# Heavy deps (reqwest, keyring) come per-story — see FOUNDATION §2 / §6.13, E1-S6.
