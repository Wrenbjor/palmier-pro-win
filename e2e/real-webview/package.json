{
  "name": "palmier-pro-e2e-real-webview",
  "private": true,
  "version": "0.1.0",
  "description": "Layer B: real-webview e2e via tauri-driver + WebdriverIO. Drives the real WebView2 window + real Rust invoke bridge. Requires tauri-driver + a matching msedgedriver (vendored in .driver/).",
  "scripts": {
    "test": "wdio run ./wdio.conf.ts"
  },
  "devDependencies": {
    "@wdio/cli": "^9.0.0",
    "@wdio/local-runner": "^9.0.0",
    "@wdio/mocha-framework": "^9.0.0",
    "@wdio/spec-reporter": "^9.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
